import { ThemeLayoutRouter } from './theme/ThemeLayoutShell.jsx';

/**
 * Premium themed watch page — routes to one of 10 unique layout designs.
 */
export default function ThemedWatchLayout(props) {
  return <ThemeLayoutRouter {...props} />;
}
